# 🚀 MrDilz Command Package (.deb)

------------------------------------------------------------------------

# 🇬🇧 English Version

> Welcome, Terminal Warriors!\
> All MrDilz commands are ready to use. Just type in your terminal,
> press Enter, and boom! ✨\
> No complicated setup --- **Executor is ready out of the box**! 😎

## 📋 Full Command List

  -----------------------------------------------------------------------------
  Command            Description
  ------------------ ----------------------------------------------------------
  `mrdilz_setup`     ⚙️ Setup -- Configure API & Wallet

  `mrdilz_chain`     🔗 Chains -- Add / Update / Remove permit chain

  `mrdilz_eip7702`   🔌 EIP-7702 -- Add / Update / Remove EIP-7702 chain

  `approvall`        ✅ Run main approval

  `call`             ▶️ Run calldata execution

  `call2`            ▶️ Run calldata execution V2

  `executor`         👨‍💻 Execute calldata

  `relay`            🚀 Execute permit with relayer

  `uniswapv2`        🔄 Uniswap V2 swap *(Recommended)*

  `unwrap`           💸 Unwrap token

  `zk`               🌀 zkSync interaction
  -----------------------------------------------------------------------------

> 💡 Tip: All commands can be executed directly from your terminal.

------------------------------------------------------------------------

## 📥 Installation (.deb)

``` bash
git clone https://github.com/dhilz/mrdilz.git
cd mrdilz
chmod +x *.deb
sudo dpkg -i *.deb
sudo mrdilz_setup
```

------------------------------------------------------------------------

## 🗑️ Uninstall

``` bash
sudo dpkg -r mrdilz
sudo rm -rf /opt/MrDilz
```

> ⚠️ Disclaimer: Use responsibly and at your own risk.

======================================================================

# 🇮🇩 Versi Bahasa Indonesia

> Selamat datang, Sobat Terminal!\
> Semua command MrDilz siap dipakai. Ketik di terminal, tekan Enter, dan
> langsung jalan! ✨\
> Tidak perlu ribet --- **Executor sudah siap pakai**! 😎

## 📋 Daftar Command

  Command            Deskripsi
  ------------------ ---------------------------------------------------
  `mrdilz_setup`     ⚙️ Setup -- Konfigurasi API & Wallet
  `mrdilz_chain`     🔗 Chains -- Tambah / Update / Hapus chain permit
  `mrdilz_eip7702`   🔌 EIP-7702 -- Tambah / Update / Hapus chain
  `approvall`        ✅ Jalankan approval utama
  `call`             ▶️ Jalankan calldata
  `call2`            ▶️ Jalankan calldata V2
  `executor`         👨‍💻 Jalankan executor
  `relay`            🚀 Jalankan permit dengan relayer
  `uniswapv2`        🔄 Swap Uniswap V2 *(Direkomendasikan)*
  `unwrap`           💸 Unwrap token
  `zk`               🌀 Interaksi zkSync

> 💡 Tip: Semua command bisa langsung dijalankan dari terminal.

------------------------------------------------------------------------

## 📥 Cara Install (.deb)

``` bash
git clone https://github.com/dhilz/mrdilz.git
cd mrdilz
chmod +x *.deb
sudo dpkg -i *.deb
sudo mrdilz_setup
```

------------------------------------------------------------------------

## 🗑️ Cara Uninstall

``` bash
sudo dpkg -r mrdilz
sudo rm -rf /opt/MrDilz
```

> ⚠️ Gunakan software ini secara bijak dan penuh tanggung jawab.
